/** Matches Prisma Frequency enum. */
export enum Frequency {
  VERY_COMMON = "VERY_COMMON",
  COMMON = "COMMON",
  OCCASIONAL = "OCCASIONAL",
  RARE = "RARE",
}

/** Matches Prisma Register enum. */
export enum Register {
  CASUAL = "CASUAL",
  NEUTRAL = "NEUTRAL",
  FORMAL = "FORMAL",
}
